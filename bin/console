#!/usr/bin/env ruby

require "bundler/setup"
require "gem_finder"

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier.

require "pry"
GemFinder::Setup.validate_yml!
Pry.start
